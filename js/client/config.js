var config = {
    product: {
        version: 0, // will not update untill pre beta state
        state: "pre beta",
        name: "mapper"
    },
    connection: {
        soket: {
            host: "127.0.0.1",
            proto: "ws",
            port: "1400"

        }
    }
};

window.config = config;